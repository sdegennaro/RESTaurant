<h1>Staff List</h1>

<%= link_to "Add a new staff member", new_waitstaff_path %>

<% @waitstaffs.each do |staff| %>
  <h2><%= link_to( staff.name, waitstaff_path(staff.id) ) %></h2>

  <p>Name: <%= staff.name%></p>

<% end %>
